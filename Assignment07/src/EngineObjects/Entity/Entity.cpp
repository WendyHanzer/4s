#include "Entity.h"

//Sorry, really do not know how to deal with event function

/*
			eventTick.exec(delta);
			Event<double> eventTick;

			eventDestroy.exec(this);
			Event<Entity*> eventDestroy;
*/

Entity::Entity()
{

} //Entity